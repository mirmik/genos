#ifndef IGRIS_STD_SSTREAM_H
#define IGRIS_STD_SSTREAM_H
#include "igris_std_config.hpp"

#include <iostream>
#include <string>

namespace IGRIS_STD_NS
{
    template <class CharT,
              class Traits = IGRIS_STD_NS::char_traits<CharT>,
              class Allocator = IGRIS_STD_NS::allocator<CharT>>
    class basic_stringstream : virtual public IGRIS_STD_NS::basic_ostream<CharT, Traits>,
                               virtual public IGRIS_STD_NS::basic_istream<CharT, Traits>
    {
        IGRIS_STD_NS::basic_string<CharT, Traits, Allocator> _str;

    public:
        using char_type = CharT;
        using traits_type = Traits;
        using allocator_type = Allocator;
        using string_type = IGRIS_STD_NS::basic_string<CharT, Traits, Allocator>;

        basic_stringstream() : IGRIS_STD_NS::basic_ostream<CharT, Traits>() {}
        basic_stringstream(const string_type &str)
            : IGRIS_STD_NS::basic_ostream<CharT, Traits>(), _str(str)
        {
        }

        void clear()
        {
            _str.clear();
        }

        string_type str() const
        {
            return _str;
        }

        void str(const string_type &str)
        {
            _str = str;
        }

        bool eof() const
        {
            return false;
        }

        bool fail() const
        {
            return false;
        }

        bool bad() const
        {
            return false;
        }

        bool good() const
        {
            return true;
        }
    };

    using ostringstream = basic_stringstream<char>;
    using stringstream = basic_stringstream<char>;
    using istringstream = basic_stringstream<char>;
}

#endif
