Module("genos.board.crius", {
	sources = {
		directory = "src",
		cc = "board.c",
	},

	modules = {
		{ name = "genos.arch.atmega2560" }
	}
})

Module("genos.include.board.crius", {
	includePaths = "include",

	modules = {
		{ name = "genos.board.crius" }
	}
})