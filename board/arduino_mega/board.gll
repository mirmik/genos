Module("genos.board.arduino_mega", {
	sources = {
		directory = "src",
		cc = "board.c",
	},

	depends = {
		"genos.arch.atmega2560"
	}
})

Module("genos.board.arduino_mega.utility", {
	sources = {
		cxx = "utility.cpp",
	},
})

Module("genos.include.board.arduino_mega", {
	includePaths = "include"
})