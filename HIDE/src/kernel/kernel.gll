Implementation("genos.atomic_section", "disable_irqs", {
	sources = {
		cc  = "atomic_section_irqs.c"
	}
})

Implementation("genos.atomic_section", "std::mutex", {
	sources = {
		cxx  = "atomic_section_mutex.cpp"
	}
})

Module("genos.kernel.standart_glue", {
	sources = {
		cxx  = "KernelGlue.cpp"
	},
	depends = "genos.atomic_section genos.kernel.scheduler"
}) 

Module("genos.kernel.pool", {
	sources = {
		cxx  = "pool/Pool.cpp"
	},
}) 